mutation CreateExercise($input: ExerciseInput!) {
  createExercise(input: $input) {
    hasError
    errors {
      title
      questions {
        title
        isCorrect
        answers {
          text
          isCorrect
        }
      }
    }
    exercise {
      ...exerciseSnippet
    }
  }
}
